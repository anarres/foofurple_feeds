<div class='stream_item'>

    <li


</div> <!--.stream_item-->


    foo += "<h2><a href='%s'>%s</a> (remove stream)</h2><ul>" % (s.get_filename(), s.title) 
    for f in s.feeds_list:
        foo += "<li>%s: %s (remove feed)</li>" % (f['name'], f['url'])
    foo += "</ul>"
