<div class='stream'>

    <br>

    <h2><a class="streamTitleList" id="{{ stream_title }}List" href='{{ stream_url }}'>{{ stream_title }}</a> <span class='remove_stream'>(remove stream)</span></h2>

    <ul id="{{ stream_title }}ListReally">

        {% for f in stream_feeds %}

        <li><a class='{{ stream_title }}' id='{{ f.name }}' href='{{ f.url }}'>{{ f.name }}</a> <span class='remove_feed'>(remove feed)</span></li>

        {% endfor %}

    </ul>

    <button class='addFeedToStream' id='{{ stream_title }}'>
        Add a feed to this stream
    </button>


    <br><br>

</div> <!--.stream-->
